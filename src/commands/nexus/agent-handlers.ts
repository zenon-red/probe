/** @deprecated Logic moved to src/commands/nexus/agent/*.ts — file retained until Wave 1 handler cleanup. */
export { currentAgentForIdentity, normalizeCapabilities, renderAgentBio } from "./agent/shared.js";
